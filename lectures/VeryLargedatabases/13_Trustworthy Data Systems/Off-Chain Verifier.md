A component that checks or validates data, computations or transactions outside the [[Blockchains]] and then provides a verifiable result to the on-chain system.

Main goal
- Peers can detect other potentially misbehaving peers
	- The [[Storage Layer]] of a remote peer could potentially drop puts or return value for a read operation
- ![[Online Verification]]
- ![[Offline Verification]]
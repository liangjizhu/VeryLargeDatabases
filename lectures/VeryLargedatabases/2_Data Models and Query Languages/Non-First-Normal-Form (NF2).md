Describes data structures that don't meet the strict rules of [[Relational Database]] , allowing attributes to hold multiple values or nested structures (like [[Tables]] within [[Tables]]) instead of just single, atomic values, often seen in older systems or [[NoSQL]], providing flexibility but requiring complex handling for data consistency compared to normalised, [[First Normal Form (1NF)]] models
A technique used to merge two diverged versions of data by comparing them against a common base version.
You have:
- Base version (original)
- Version A (modified copy)
- Version B (modified copy)
The merge process:
- Applies changes from A and B relative to the base
- Detects conflicts if both modify the same part differently
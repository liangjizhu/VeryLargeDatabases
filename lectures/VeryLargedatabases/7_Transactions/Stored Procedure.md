A predefined, server-side program (often written in [[SQL]] or a [[Procedural SQL Language]]) that runs inside the database engine. Instead of sending many individual [[SQL]] statements from the client, the client sends one call to the stored procedure and the logic executes close to the data.

![[Pros and Cons of Stored Procedures]]
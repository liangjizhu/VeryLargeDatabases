A correctness property that states that “something good eventually happens.”
It guarantees that the system makes progress and does not stall forever.
Unlike safety, liveness violations are temporary and can be fixed if conditions improve.